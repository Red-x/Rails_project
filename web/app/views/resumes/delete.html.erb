<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="resume delete">
  <h2>Delete resume</h2>

  <%= form_for(:resume , :url => {:action => 'destroy', :id => @resume.id } ) do |f| %>

    <p>Are you sure you want to permanently delete this resume?</p>

    <p class="reference-name"><%= @resume.name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete resume") %>
    </div>

  <% end %>
</div>
